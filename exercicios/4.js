//Crie uma variável com um número e use o operador ternário para verificar se é par ou ímpar.

let num = 23654265;
let resto = num % 2;
let msg = resto == 0 ? "Numero par" : "numero impar";
console.log(msg);